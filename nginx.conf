server {
    listen 80;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    # Log all requests to stdout (visible in Coolify logs)
    access_log /dev/stdout;
    error_log /dev/stderr;

    location / {
        try_files $uri $uri/ /index.html;
    }
}
